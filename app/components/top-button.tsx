import { Button } from "@mui/material";

const TopButton = () => {
  return (
    <Button href="/" sx={{ color: "#5f82ff", fontSize: 16 }}>
      ＜ Topへ戻る
    </Button>
  );
};

export default TopButton;
