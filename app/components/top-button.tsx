import { Button } from "@mui/material";

const TopButton = () => {
  return (
    <Button href="/" sx={{ color: "#5f82ff", ml: { xs: 1, md: 3 } }}>
      ＜ Topへ戻る
    </Button>
  );
};

export default TopButton;
